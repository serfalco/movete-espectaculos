/* --- Destacado editorial de la semana (lo under) --- */
.destacado-under {
  margin-top: 24px;
}
.destacado-eyebrow {
  font-family: var(--font-action);
  text-transform: uppercase;
  letter-spacing: .08em;
  font-size: .78rem;
  font-weight: 900;
  color: var(--accent-text);
  margin: 0 0 8px;
}
.destacado-card {
  background: var(--card);
  border: 2px solid var(--ink);
  border-left: 8px solid var(--accent);
  border-radius: var(--radius);
  padding: 22px 24px;
  box-shadow: 0 14px 35px rgba(14, 13, 16, .08);
}
.destacado-when {
  color: var(--accent-text);
  font-family: var(--font-action);
  text-transform: uppercase;
  letter-spacing: .04em;
  font-weight: 950;
  font-size: .8rem;
  margin: 0 0 6px;
}
.destacado-title {
  font-size: clamp(1.5rem, 3.5vw, 2.1rem);
  margin: 0 0 6px;
}
.destacado-title a { text-decoration: none; }
.destacado-venue {
  color: var(--muted);
  font-weight: 700;
  margin: 0 0 10px;
}
.destacado-note {
  color: var(--muted);
  font-size: .82rem;
  font-style: italic;
  border-top: 1px solid var(--line);
  margin: 12px 0 0;
  padding-top: 10px;
}
